version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: wed-love-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: wedlove
      POSTGRES_PASSWORD: wedlove123
      POSTGRES_DB: wedlove
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U wedlove"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: wed-love-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data:
